export const API_BASE_URL = import.meta.env.MODE === 'production' 
  ? 'https://helix-26.onrender.com' 
  : '';

export const API_URL = import.meta.env.MODE === 'production' 
  ? 'https://helix-26.onrender.com' 
  : '';

